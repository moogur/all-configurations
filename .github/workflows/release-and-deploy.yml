name: Release, deploy and update task

on:
  push:
    tags:
      - v*.*.*

jobs:
  kanboard:
    uses: moogur/all-workflows/.github/workflows/kanboard.yml@master
    secrets: inherit
    with:
      kanboard_columns: '81,82,83,84,85,86,87'
      project_type: 'multi_branch'
      event_type: 'deploy'
